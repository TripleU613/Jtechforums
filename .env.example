VITE_FORUM_API_BASE_URL=/api
VITE_FORUM_URL=https://forums.jtechforums.org
VITE_DISCOURSE_GUIDES_PATH=guides
VITE_RECAPTCHA_SITE_KEY=6LcKrggsAAAAAOVbLq4ZQ_bHpVAnlfNFAJQAhoRz
# Optional: uncomment for local-only leaderboard fetches (dev server only)
# VITE_DISCOURSE_API_KEY=your-local-discourse-key
# VITE_DISCOURSE_API_USERNAME=system
# Override the remote fallback base used in dev (defaults to deployed Cloud Run URL)
# VITE_FORUM_REMOTE_FALLBACK=https://forumapi-vvtyqkmgxa-uc.a.run.app
# VITE_FORUM_USE_MOCK accepts:
#   true/always/force -> always use mocks
#   auto/fallback     -> only use mocks if the live request fails (dev only)
#   (unset/default)   -> never use mocks
# VITE_FORUM_USE_MOCK=
